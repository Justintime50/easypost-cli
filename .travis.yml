language: bash

before_scripts:
    - ./build.sh 1.0.0 bash

scripts:
    # Test Default Bash
    - shellcheck *.sh
    - shellcheck executables/unix/ep
    # Test SH
    - shellcheck *.sh --shell=sh
    - shellcheck executables/unix/ep --shell=sh
    # Test Dash
    - shellcheck *.sh --shell=dash
    - shellcheck executables/unix/ep --shell=dash
    # Test KSH
    - shellcheck *.sh --shell=ksh
    - shellcheck executables/unix/ep --shell=ksh

    # TODO: Add unit tests here when working
